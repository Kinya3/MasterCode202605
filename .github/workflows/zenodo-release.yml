name: Prepare Zenodo JSON on Release

# ★リリースだけでなく、画面から「手動」でも実行できるようにトリガーを追加
on:
  release:
    types: [published, edited]
  workflow_dispatch: 

jobs:
  build-json:
    runs-on: ubuntu-latest
    steps:
      # 【ステップ①】リポジトリのファイルを読み込む（ここが成功するかチェック）
      - name: 1. Checkout code
        uses: actions/checkout@v4

      # 【ステップ②】ファイルが正しく存在しているか、中身を確認する（画面に中身をプリント）
      - name: 2. Check source files
        run: |
          echo "=== HTMLファイルの中身を確認します ==="
          cat .github/workflows/zenodo_description.html || echo "HTMLファイルが見つかりません！"
          
          echo "=== 補足テキストの中身を確認します ==="
          cat .github/workflows/zenodo_keywords.txt || echo "Keywordsファイルが見つかりません！"

      # 【ステップ③】合体ロボット（jq）を動かして、画面に結果を表示する（保存はしない）
      - name: 3. Generate and View .zenodo.json
        run: |
          # =========================================================
          # 1. 基本設定（ご自身の情報に書き換えてください）
          # =========================================================
          TITLE="原本工学 (MCE)シリーズ論文 平和の実装"
          CREATORS='[{"name": "：田中 欽也（Tanaka Kinya）", "affiliation": "MirandaWorks株式会社"}]'
          LICENSE="cc-by-nc-nd-4.0"
          UPLOAD_TYPE="publication"
          PUBLICATION_TYPE="preprint"
          NOTES="翻訳の網目による情報の劣化を避けるため、解釈に相違が生じた場合は日本文の記述を原本（正）とする。"

          # =========================================================
          # 2. workflowsフォルダ内の各ファイルからデータを読み込み
          # =========================================================
          DESCRIPTION=$(jq -Rs . < .github/workflows/zenodo_description.html)
          KEYWORDS=$(jq -R . < .github/workflows/zenodo_keywords.txt | jq -s .)
          RELATED_IDENTIFIERS="[]"

          echo "=== .zenodo.json を組み立てます ==="
          jq -n \
            --arg title "$TITLE" \
            --argjson creators "$CREATORS" \
            --arg license "$LICENSE" \
            --arg upload_type "$UPLOAD_TYPE" \
            --arg publication_type "$PUBLICATION_TYPE" \
            --arg notes "$NOTES" \
            --argjson description "$DESCRIPTION" \
            --argjson keywords "$KEYWORDS" \
            --argjson related_identifiers "$RELATED_IDENTIFIERS" \
            '{
              title: $title, 
              creators: $creators, 
              license: $license, 
              upload_type: $upload_type, 
              publication_type: $publication_type, 
              notes: $notes, 
              description: $description, 
              keywords: $keywords,
              related_identifiers: $related_identifiers
            }'








